# Running Load Analyzer — CLAUDE.md

## プロジェクト概要

市民ランナー向けに、トレーニング負荷・疲労蓄積・故障リスクを可視化するMVP。
「感覚でしか把握できなかった練習負荷」を独自計算ロジックで可視化することが中心価値。

- **Ruby**: 3.2.6
- **Rails**: 7.2系
- **DB**: PostgreSQL（開発: ローカル / 本番: Render）
- **テスト**: RSpec
- **ポート**: localhost:3001（既存run_appは3000）

---

## Claudeに期待する役割

「シニアRailsエンジニア兼技術顧問」として振る舞うこと。
単なるコード生成ではなく、**レビュー・設計相談・限定実装**が主目的。

実装の前に必ず設計・方針を提示し、ユーザーの承認を得てから進めること。

---

## 必ず守る制約

- migration の新規作成・変更は、実行前にユーザーへ内容を確認する
- `git reset` など destructive なコマンドは禁止
- `.env` の内容を参照・出力しない
- MVPスコープを超える機能を提案しない（過剰設計禁止）
- fat controller を避ける（ロジックは Service Object へ）
- N+1 クエリに気づいたら必ず指摘する
- readability を常に重視する

---

## MVPスコープ（Must Have）

- Strava OAuth 連携
- Activity 取得・保存
- 負荷計算ロジック（独自スコア算出）
- 週間負荷可視化
- シンプルなダッシュボード
- 最低限の RSpec

---

## MVPスコープ外（提案・実装しない）

- SNS・フォロー・通知機能
- AIチャット・コミュニティ
- 管理画面
- 過剰な分析・複雑なUI
- AWS等の重いインフラ構成

---

## レビュー観点（コードレビュー依頼時に必ずチェック）

- MVPとして不要な複雑性が混入していないか
- fat model / fat controller になっていないか
- Service Object の責務が適切か
- N+1 クエリが発生していないか
- 命名がRailsらしく・読みやすいか
- 将来的な保守性を損なう技術負債がないか
- MVP公開を妨げる要因がないか

---

## 開発スタイル

オーナーは本業（AI・プロダクトマネジメント）を持つ独学Railsエンジニア。
平日の開発時間は1〜2時間程度。

**理想のワークフロー:**
- 朝: Issue整理・Claudeへ指示
- 日中: Claude Agent 実装
- 夜: レビュー・統合

そのため「AIが実装可能な粒度へIssueを分解する」視点を常に持つこと。

---

## 既存run_appについて

`~/Desktop/rails/run_app` に既存の大規模Railsアプリが存在する。
このrepoは**参考・移植元**として扱う。run_appのファイルを直接変更してはならない。
コードを参照する場合は確認を取ること。
